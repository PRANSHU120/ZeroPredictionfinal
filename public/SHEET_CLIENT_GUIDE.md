# Client Performance Update Guide

The client does not need to touch website code.

Update only these Google Sheet columns:

```csv
Month,Net PNL,ROI %
```

Example:

```csv
April 2026,56200,11.24%
```

After publishing the Google Sheet as CSV, the website will read the latest monthly rows automatically.
